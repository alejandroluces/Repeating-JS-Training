// 28) Ejercicio: Verificar si un número es positivo o negativo, pero no cero
// Planteamiento: Escribe una función que tome como entrada un número y determine si es
//  positivo o negativo, pero no cero.
// Entrada: Un número, por ejemplo: -5
// Salida: true si el número es positivo o negativo, pero no cero, false en caso contrario.

const valNumbNegOrPos=(num)=>{

    return num>0 || num<0

}

console.log(valNumbNegOrPos(-5));
