// Ejercicio 320
// Verificar si todos los elementos de un Array son strings

// Entrada: ['manzana', 'pera', 42, 'uva']
// Salida: false


const element = ['manzana', 'pera', 42, 'uva']
const validateIfAllEleleent = (arrAnimales) =>{

    return arrAnimales

}

console.log(validateIfAllEleleent(element));
